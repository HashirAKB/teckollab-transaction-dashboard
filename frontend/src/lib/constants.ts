export const DASHBOARD_CONSTANTS = {
    DEBOUNCE_DELAY: 500,
    DEFAULT_PAGE: 1,
    DEFAULT_LIMIT: 10,
} as const;

export const APP_CONSTANTS = {
    THEME: {
      DEFAULT: 'light',
      STORAGE_KEY: 'app-theme'
    }
} as const;